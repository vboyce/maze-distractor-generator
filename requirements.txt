# Core dependencies for distract.py (transformers backends).
# For exact versions of a known-good environment (Linux, CUDA), see requirements-lock.txt.
# On a machine without an NVIDIA GPU, install the CPU build of torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
transformers>=4.40
wordfreq>=3.0
numpy>=1.26
pytest>=8
